ИНТЕРФЕЙС
---------
1. Убрать префикс BEM- потому что это не префиксы `BEM!` (check)
2. BEM-navigation__menu => BEM-navigation-menu `BEM!` (check)
    - в элементе не может быть блоков
    - в элементе не может быть элементов
3. Для видео колонки справа ul>li>figure (check)
4. item__active => item__link_active `BEM!` (check)
5. Для картинки нужно добавить class так как ее нет в логике BEM (check)
6. Для жирных шрифтов писать font-weight: bold
7. Навигаци карусели вправо/влево это button а не span (check)
8. Так как будет менятся JavaScript-ом `<span>7</span>/10` (check)
9. Main news - списки картинок (check)
10. Линия должна подстраиватся Main News и линия должны быть респонив (линю надо выравнивать по-другому)
11. Иконки фото/видео не надо класть в DOM сразу с классом hidden, нужно просто не класть в DOM а поудалять с верски (check)
12. Last news и Feed news сделать одинаковыми блоками (check)
13. BEM-news-list__link_name => BEM-news-list__source `BEM!` (check)
14. div.image__wrapper > img и span оба в нем `BEM!` (check)
15. Убрать margin-bottom у последнего feed news (check)
16. Не фиксировать высоту у aside (высоту особенно никогда не фиксировать)
17. Не фиксировать высоту в асайд видео элементах (check)
18. Текст между текстом и картинкой выравнивать марджином картинки
19. More videos убрать абсолютное позиционирование и свойство bottom (check)
20. Обычно img кладется в div контейнер (обычно с классом outer) и если он понадобиться не надо будет лезть в HTML (check)
21. Главная новость feed должна быть тоже в li (check)

КОД
---
1. Блок карусель в одном файле содержить все остальны блоки carousel-control, carousel-item...(в папке carousel лежит один файл carousel.scss) `BEM!` (check)
2. height лучше не фиксировать в миксинах, height лучше не фиксировать нигде! (очень редко где ее надо фиксировать)
3. Более привычный content-box
4. `</div><!-- переход на новую строку --><div> альтернативный способ </div переход на новую строку ><div>`
5. Текст можно спокойно класть DIV или любой другой пожходящий элемент (check)