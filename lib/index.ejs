<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="shortcut icon" type="image/x-icon" href="./favicon.ico">
    <link rel="stylesheet" href="./css/normalize.css">
    <link rel="stylesheet" href="./css/main.css">
    <title>Methodologies and Frameworks</title>
</head>
<body class="BEM-layout">
    <main class="BEM-layout__main">
        <% include ./views/navigation.ejs %>
        <% include ./views/carousel.ejs %>
        <% include ./views/news.ejs %>
    </main>
    <aside class="BEM-layout__aside">
        <% include ./views/videos.ejs %>
    </aside>
    <script>
        /**
         * Creates random colored border around each html element
         */
        [].forEach.call( document.querySelectorAll( "*" ), function ( a ) {
            a.style.outline = "1px solid #" + (~~(Math.random() * (1 << 24))).toString( 16 )
        } );
    </script>
</body>
</html>